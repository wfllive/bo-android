# React + Webpack (без Vite)

Запуск и разработка:

```bash
npm install
npm run dev
```

Сборка продакшн:

```bash
npm run build
```

Структура:
- `src/` — исходники React
- `public/` — HTML-шаблон
- `webpack.config.js` — конфигурация сборщика
- `.babelrc` — настройки Babel